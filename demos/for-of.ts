
namespace Demos.ForOf {

    // Iterating elements of an array

    // var myArray = ['item1', 'item2', 'item3'];
    // for (var key in myArray) {
    //     console.log(key);
    // }
    // for (var value of myArray) {
    //     console.log(value);
    // }

    // Doesn't work with objects :(
    // as objects don't implement the iterable protocol
    // see https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Iteration_protocols#iterable

    // var myObject = {
    //     value1: 1,
    //     value2: 2,
    //     value3: 3
    // };
    // for (var value in myObject) {
    //     console.log(value);
    // }
    // for (var value of myObject) {
    //     console.log(value);
    // }
}
